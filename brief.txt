Callback Functions in javascript
_____________________________________________________

the function that is send into another fucntion is called Callback fucntion

since the since the function send can be called back into the other fucntion latter on therefore
it is called Callback fucntion


//this gives the concept of not blocking the main thread when writing the JS code
//since javascript is synchronous and runs one code line at a time therefore blocking the main thread 
//will cause alot of problemos




_____________________________________________________Since
we have the Callback fucntions and settimeout we can now perform asynchronous coding in out 
JS program.

