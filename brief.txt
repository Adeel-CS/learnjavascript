Now Learning Let and Const

_________First remember the Concept of hoisting ______________

In which you can call a variable before it is declared
You get Undefined


_________________


Now let is also hoisted but with a different method
It gives error

______________


Temporal Deadzone is the time between the let is hoisted and the time 
it get assigned a value.

_________________
______________

Now if we try to get window that is the object created and everything inside is its 

we can get window.var(variables)  
but not window.let/Const
since both these are assigned in a seperate script and area

Window is the global object